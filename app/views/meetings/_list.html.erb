 <% if users.length >= 1 %>
  <% users.each_with_index do |user, index| %>
    <input
            data-form-steps-target="checkbox"
            id= "name<%=index%>"
            type="checkbox"
            name="users[]"
            value= "<%= user.name%>">
      <label for="name<%=index%>">
        <%= image_tag user.photo.key ? cl_image_path(user.photo.key) :  "gorilla.jpg", class: "avatar"%>
        <%= user.name %>
      </label>
    <% end %>
  <% end %>
